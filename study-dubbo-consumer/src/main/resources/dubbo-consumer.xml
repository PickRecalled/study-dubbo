<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:dubbo="http://dubbo.apache.org/schema/dubbo"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.3.xsd
		http://dubbo.apache.org/schema/dubbo http://dubbo.apache.org/schema/dubbo/dubbo.xsd
		http://code.alibabatech.com/schema/dubbo http://code.alibabatech.com/schema/dubbo/dubbo.xsd">
    <context:component-scan base-package="com.pickrecalled.service"></context:component-scan>

    <!--指定服务名-->
    <dubbo:application name="study-dubbo-consumer"></dubbo:application>

    <!--指定消费中心地址-->
    <dubbo:registry address="zookeeper://10.211.55.5:2181"></dubbo:registry>

    <!--声明需要调用的远程服务的接口；生成远程服务代理-->
    <dubbo:reference id="userService" interface="com.pickrecalled.service.UserService" version="1.0.0" timeout="3000"
                     retries="3" stub="com.pickrecalled.service.UserServiceStub"></dubbo:reference>

    <!--连接监控中心指定从注册中心发现监控中心地址，否则直连监控中心-->
    <dubbo:monitor protocol="registry"></dubbo:monitor>
</beans>

